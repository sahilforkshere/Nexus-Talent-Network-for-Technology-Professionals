supergraph:
  listen: 0.0.0.0:4000
  introspection: true

sandbox:
  enabled: true

homepage:
  enabled: false

cors:
  allow_any_origin: true

headers:
  all:
    request:
      - propagate:
          named: Authorization
